const mongoose = require("mongoose");
const {Schema} = require("mongoose");

const {{capitalizeFirst results.schemaName}}Schema = new mongoose.Schema({
  {{#each results.attributes}}
    {{this.attribute}}: {{{attributeFormat this}}},
  {{/each}}
});

const {{capitalizeFirst results.schemaName}} = mongoose.model('{{capitalizeFirst results.schemaName}}', {{capitalizeFirst results.schemaName}}Schema);

module.exports = {{capitalizeFirst results.schemaName}}